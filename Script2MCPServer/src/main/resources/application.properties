# 服务器配置
server.port=8080

# 日志配置
logging.level.root=INFO
logging.level.com.example.scripttomcpserver=DEBUG

# SSH配置
ssh.connection.timeout=5000

# Docker配置
docker.host=http://localhost:2375
docker.api.version=1.41

# MCP Server配置
mcp.server.template.directory=classpath:templates/